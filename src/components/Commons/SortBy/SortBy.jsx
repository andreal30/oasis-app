// export const handleSortChange = (field, order) => {
//     const sorted = [...filteredUsers].sort((a, b) => {
//       const valA = field === "flatCount" ? a.flatCount : a[field];
//       const valB = field === "flatCount" ? b.flatCount : b[field];

//       if (valA < valB) return order === "asc" ? -1 : 1;
//       if (valA > valB) return order === "asc" ? 1 : -1;
//       return 0; // Equal values
//     });

//     setFilteredUsers(sorted); // Update the filtered users with the sorted list
//   };
